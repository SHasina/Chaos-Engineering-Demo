apiVersion: v1
kind: Service
metadata:
  name: order-api-svc
  namespace: resilience-lab
spec:
  type: NodePort
  selector:
    app: order-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
