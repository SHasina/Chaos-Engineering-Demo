apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-api
  namespace: resilience-lab
  labels:
    app: payment-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-api
  template:
    metadata:
      labels:
        app: payment-api
    spec:
      containers:
        - name: payment-api
          image: payment-api:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          env:
            - name: INVENTORY_SERVICE_URL
              value: "http://inventory-api-svc.resilience-lab.svc.cluster.local:8000"
            - name: RESILIENT_MODE
              value: "true"
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
