apiVersion: v1
kind: Service
metadata:
  name: inventory-api-svc
  namespace: resilience-lab
spec:
  type: ClusterIP
  selector:
    app: inventory-api
  ports:
    - port: 8000
      targetPort: 8000
